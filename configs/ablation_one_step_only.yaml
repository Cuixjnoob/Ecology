extends: base.yaml

experiment:
  name: ablation_one_step_only
  output_dir: runs/ablation_one_step_only

loss:
  lambda_rollout: 0.0

train:
  stage_a_epochs: 30
  stage_b_epochs: 0
  max_rollout_horizon: 1
